{#{% include 'default/_form.html.twig' with {
    page_title: 'title.user_register',
    attr: {
        method: 'POST',
        action: url('user_register')
    },
    back_to_list_path: 'security_login',
} %}#}
{% extends 'base.html.twig' %}



{% block body %}



    {{ form_start(form1, { method: 'POST'|default('POST'), action: url('user_register') }) }}
    {{ form_start(form2, { method: 'POST'|default('POST'), action: url('user_register') }) }}
    {% if false == form1.vars.valid %}
    {% if false == form2.vars.valid %}
        {% include 'default/_single_flash_message.html.twig' with {
            type: 'danger',
            message: 'message.form_error'|trans,
        }%}
    {% endif %}
    {% endif %}
    {{ form_widget(form1) }}
    {{ form_widget(form2) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ submit_label|default('action.save'|trans) }}" class="btn btn-primary" />
    </div>
    {% if back_to_list_path|default(false) %}
        <div class="form-group row float-sm-left">
            <a href="{{ url(back_to_list_path) }}" class="btn btn-link">
                {{ 'action.back_to_list'|trans }}
            </a>
        </div>
    {% endif %}
    {{ form_end(form1) }}
    {{ form_end(form2) }}

{% endblock %}