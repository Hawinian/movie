{% include 'default/_form.html.twig' with {
    page_title: 'title.grant_admin',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('admin_grant_role', {'id': user.id}),
    },
    back_to_list_path: 'admin_users',
} %}