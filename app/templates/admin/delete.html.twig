{% include 'default/_form.html.twig' with {
    page_title: 'title.movie_delete',
    page_title_param: {'%id%': movie.id},
    attr: {
        method: 'DELETE',
        action: url('admin_delete', {'id': movie.id}),
    },
    back_to_list_path: 'admin_index',
} %}