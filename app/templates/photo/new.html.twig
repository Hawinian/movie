{% include 'default/_form.html.twig' with {
    page_title: 'title.movie_new',
    attr: {
        method: 'POST',
        action: url('photo_new', {id: movie.getId()})
    },
    back_to_list_path: 'user_index',
} %}